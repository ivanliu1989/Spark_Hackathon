package utilClasses

/**
 * @author ivanliu
 */
class Customer {
  //Lets initialize indexes to map to corresponding column names so that we can access the column by name rather than ordinal value

  val customerId: Int = 0

  val customerNm: Int = 1

  val amount: Int = 2

} //end of Object Customer